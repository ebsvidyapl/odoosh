<odoo>
    <data>

        <!-- Restrict CRM Sales Teams menu to Management only -->
        <record id="crm.crm_team_menu_config" model="ir.ui.menu">
            <field name="groups_id" eval="[(6, 0, [ref('employee_management.group_management')])]"/>
        </record>

    </data>
</odoo>