<odoo>

    <record id="view_landed_cost_form_customs" model="ir.ui.view">
        <field name="name">stock.landed.cost.form.customs</field>
        <field name="model">stock.landed.cost</field>
        <field name="inherit_id" ref="stock_landed_costs.view_stock_landed_cost_form"/>

        <field name="arch" type="xml">

            <!-- ✅ SAFEST WAY: add fields AFTER the one2many -->
            <xpath expr="//field[@name='valuation_adjustment_lines']" position="after">

                <field name="valuation_adjustment_lines" invisible="1"/>

            </xpath>

            <!-- ✅ ADD BUTTON ONLY -->
            <xpath expr="//header" position="inside">
                <button name="%(action_customs_report)d"
                        string="Print Customs Report"
                        type="action"
                        class="btn-primary"/>
            </xpath>

        </field>
    </record>

</odoo>